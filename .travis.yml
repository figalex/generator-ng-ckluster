language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ng-ckluster' ]; then cd .. && eval "mv $currentfolder generator-ng-ckluster" && cd generator-ng-ckluster; fi

